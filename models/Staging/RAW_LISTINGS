select id as listing_id,
       listing_url,
       initcap(name) as listing_name,
       room_type,
       minimum_nights,
       host_id,
       replace(price , '$', '') as price,
       created_at as listing_created,
       updated_at as listing_updated from airbnb.raw.raw_listings